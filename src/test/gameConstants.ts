import { bufferSeconds, catCountDefault, durationDefault, roundsDefault } from '../game/constants';

export const DEFAULT_DRAW_COUNT = catCountDefault;
export const DEFAULT_TIMER_SECONDS = durationDefault;
export const DEFAULT_ROUNDS = roundsDefault;
export const BUFFER_SECONDS = bufferSeconds;
